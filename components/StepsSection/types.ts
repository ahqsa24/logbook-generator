/**
 * Local types for StepsSection module
 */

export interface StepsSectionState {
    step: number;
    aktivitasId: string;
    cookies: any;
    entries: any[];
    results: any[];
    timestamp?: string;
}
